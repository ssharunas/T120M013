program 'abc';
var a,b,c: integer;
var d,e: double;
  begin
	input ( a, b );
	c := a * b;
	output ( c );
	d := a + tanh(c);
	output ( d );
	output ( d, tanh(d), tanh(2), tanh(1.5), tanh(a) );
end;
